# Unity Jenkins pipeline

To make use of the shared library you must use the following sample pipeline script.
```
@Library('unity-jenkins-pipeline')_
def pipelineParams = {}
node("master") {
    checkout scm
    pipelineParams = readJSON file: 'jenkins-files/pipeline_params.json'
}
StandaloneUnityPipeline(pipelineParams)

```

Configure the following json file:
